#lang info

;; 2015-04-28:
;; Not sure exactly what we need here, but these settings
;; seem like a good guess

(define deps '("base"))
;(define build-deps '("scribble-lib" "racket-docs"))
(define pkg-desc "Library for SICM")
(define pkg-authors '(ben ben))
(define version "0.0")
